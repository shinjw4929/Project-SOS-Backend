# 하네스 엔지니어링 리팩토링 — 통합 체크리스트

## Context

Phase 1~5 구현 완료로 Room/Chat Server, 로깅 파이프라인이 모두 동작하는 상태. Unity 프로젝트(Project-SOS)에서 검증된 하네스 엔지니어링 구조를 백엔드 프로젝트에 적용한다.

**목표**: Skills 14개 체계 + Docs SSOT 구조를 백엔드 C++ 맥락에 맞게 구축하여, 구현/디버그/계획/리뷰 워크플로우를 자동화한다.

**참조**: `C:\Users\sjw49\Unity Projects\Project-SOS\.claude\skills` (Unity 프로젝트 원본)

---

## 현재 상태 분석

### Skills 현황 (5/14)

| 스킬 | 상태 | 비고 |
|------|------|------|
| commit | 존재 | 경로/품질 리뷰 필요 |
| review-code | 존재 | 경로/품질 리뷰 필요 |
| review-comments | 존재 | 경로/품질 리뷰 필요 |
| review-plan | 존재 | 경로/품질 리뷰 필요 |
| update-docs | 존재 | 경로/품질 리뷰 필요 |
| SKILLS.md | 완료 | 티어 계층/워크플로우 오케스트레이션 |
| implement | 완료 | Tier 1 핵심 |
| debug | 완료 | Tier 1 핵심 |
| analyze | 완료 | 독립 도구 |
| build | 완료 | Tier 2 검증 |
| test | 완료 | Tier 2 검증 |
| plan-create | 완료 | Tier 1 |
| plan-edit | 완료 | 독립 도구 |
| plan-execute | 완료 | Tier 1 |

### Docs 현황

| 항목 | 상태 | 문제 |
|------|------|------|
| Documentation-Checklist.md | 완료 | CLAUDE.md에 인라인 — SSOT 위반 |
| Checklists/ | 완료 | pattern-search-guide, review-code-checklist 없음 |
| Systems/ | 완료 | 모듈별 상세 문서 없음 (common, room, chat, infra) |
| 계획-작성-가이드.md | 완료 | 계획 템플릿/구조 미정의 |
| Plans/Completed/ | 완료 | 완료된 계획 아카이브 없음 |
| WorkLog/ | 완료 | Internal/구현기록이 유사 역할이나 구조 불일치 |

---

## Phase 체크리스트

### Step 1: Docs 구조 변경 — 폴더 및 파일 이동

**목표**: Unity Docs 구조와 동일한 골격 생성. 기존 문서 이동 및 빈 폴더 준비.

- [x] `docs/Checklists/` 폴더 생성
- [x] `docs/Systems/` 폴더 생성
- [x] `docs/Plans/` 폴더 생성 (기존 `docs/계획/` 대체)
- [x] `docs/Plans/Completed/` 폴더 생성
- [x] `docs/WorkLog/` 폴더 생성
- [x] 기존 `docs/계획/` 하위 파일 → `docs/Plans/`로 이동
  - `구현 우선순위.md` → `Plans/구현 우선순위.md`
  - `룸 서버/` → `Plans/룸 서버/`
  - `채팅 서버/` → `Plans/채팅 서버/`
  - `로깅/` → `Plans/로깅/`
  - `하네스 엔지니어링/` → `Plans/하네스 엔지니어링/` (본 문서 포함)
- [x] `docs/계획/로깅 통합 운영/` → `docs/Plans/Completed/로깅 통합 운영/` (이미 완료된 계획)
- [x] `docs/Internal/구현기록/` → `docs/WorkLog/`로 이동 (날짜별 폴더 구조로 재구성)
  - `2026-03-16 로깅 파이프라인 구축.md` → `WorkLog/2026-03-16/로깅 파이프라인 구축.md`
  - `2026-03-16 인프라 리뷰 및 Vector 테스트 추가.md` → `WorkLog/2026-03-16/인프라 리뷰 및 Vector 테스트 추가.md`
  - (이하 동일 패턴으로 13개 파일 재배치)
- [x] 빈 `docs/계획/`, `docs/Internal/구현기록/` 폴더 제거
- [x] `docs/Internal/` → `docs/Internal/Analysis/` 구조로 정리 (분석 문서 공간 확보)

**의존성**: 없음 (첫 번째 단계)

---

### Step 2: Docs 신규 문서 작성

**목표**: Skills가 참조할 핵심 문서 생성. CLAUDE.md에서 문서 매핑 테이블을 분리하여 SSOT 확보.

- [x] `docs/Documentation-Checklist.md` 생성
  - CLAUDE.md의 "문서 업데이트 규칙" 테이블 + "문서 작성 원칙" 이동
  - 변경 유형 → 업데이트 대상 매핑 테이블
  - Docs 폴더 구조 설명
- [x] `docs/Checklists/pattern-search-guide.md` 생성
  - C++ 구현 유형별 패턴 탐색 절차 (Boost.Asio 서버, Protobuf 핸들러, Redis 연동, 내부 채널 등)
  - 패턴 추출 항목 (구조, 에러 처리, 네이밍, Config 사용)
  - 적용 원칙 (참조 우선, 일관성, 파일 배치)
- [x] `docs/Checklists/review-code-checklist.md` 생성
  - C++ 코드 품질 (메모리, 스레드, 에러, RAII, 모던 C++, Boost.Asio)
  - 네트워크/서버 아키텍처 (프로토콜, Rate Limit, 인증, 연결 관리)
  - 인프라 설정 (Docker, ClickHouse, Vector, 환경변수)
  - 프로젝트 컨벤션 (네이밍, 폴더, 로그 형식, CMake)
- [x] `docs/Plans/계획-작성-가이드.md` 생성
  - 계획 요청 시 필요 정보 (변경 대상, 현재 문제, 원하는 결과, 제약 조건)
  - 계획 문서 구조: orchestration.md (중심 문서) + phase 파일
  - orchestration 템플릿 (문제 정의, AS-IS/TO-BE, Phase 체크리스트, 검증, 롤백)
  - Phase 파일 템플릿 (목표, 전제 조건, 작업 목록)
- [x] `docs/Systems/common 라이브러리.md` 생성
  - Codec, Logger, Config, RedisClient, SessionStore, RateLimiter 모듈 상세
- [x] `docs/Systems/Room Server.md` 생성
  - TCP 서버, ClientSession, Envelope 디스패치, Room/RoomManager, 내부 채널
- [x] `docs/Systems/Chat Server.md` 생성
  - ChatServer, ChatSession, ChatAuth, ChannelManager, InternalChannel
- [x] `docs/Systems/인프라 구성.md` 생성
  - Docker, Redis, ClickHouse, Vector, Grafana 구성 상세

**의존성**: Step 1 완료 (폴더 구조 확정 후 문서 작성)

---

### Step 3: SKILLS.md + 기존 스킬 리뷰

**목표**: 오케스트레이션 문서 생성 및 기존 5개 스킬을 새 docs 경로와 품질 기준에 맞게 업데이트.

- [x] `.claude/skills/SKILLS.md` 생성
  - 스킬 계층 구조 (Tier 1/2/독립)
  - 공유 참조 문서 테이블
  - 시나리오별 스킬 선택 가이드 (11개)
  - 워크플로우 체이닝
- [x] `commit/SKILL.md` 리뷰 및 업데이트
  - docs 경로 반영 (`docs/Plans/`, `docs/WorkLog/` 등)
- [x] `review-code/SKILL.md` 리뷰 및 업데이트
  - `docs/Checklists/review-code-checklist.md` 참조 추가
  - 검사 항목을 체크리스트 파일로 분리 (스킬 본문은 절차만)
- [x] `review-comments/SKILL.md` 리뷰 및 업데이트
  - docs 경로 반영
- [x] `review-plan/SKILL.md` 리뷰 및 업데이트
  - `docs/Checklists/review-code-checklist.md` 참조 추가
  - docs 경로 반영
- [x] `update-docs/SKILL.md` 리뷰 및 업데이트
  - `docs/Documentation-Checklist.md` 참조 추가
  - 매핑 테이블을 체크리스트 파일 참조로 대체

**의존성**: Step 2 완료 (참조 문서 존재해야 스킬이 링크 가능)

---

### Step 4: 신규 스킬 — Tier 1 (오케스트레이션)

**목표**: 핵심 워크플로우를 자동화하는 Tier 1 스킬 4개 생성.

- [x] `implement/SKILL.md` 생성
  - 패턴 탐색 (`docs/Checklists/pattern-search-guide.md` 참조)
  - 코드 작성 (CLAUDE.md Development Guidelines 준수)
  - 검증 (빌드 체크, 패턴 일관성)
  - 자동 호출: `/review-comments`, `/update-docs`
  - C++ 적응: Boost.Asio 패턴, Protobuf 핸들러, Redis 연동, 내부 채널 패턴
- [x] `debug/SKILL.md` 생성
  - 증상 수집 → 에러 분류 → 근본 원인 추적 → 수정 적용
  - C++ 에러 분류 6종: 메모리/RAII, 스레드/동시성, 네트워크 I/O, Protobuf 직렬화, Redis 연결, Docker/인프라
  - 자동 호출: `/review-comments`
- [x] `plan-create/SKILL.md` 생성
  - `docs/Plans/계획-작성-가이드.md` 로드
  - 요구사항 수집 → AS-IS 조사 → TO-BE 설계 → Phase 분할
  - orchestration.md + phase 파일 생성
  - 자동 호출: `/review-plan` (최대 3회 리뷰-수정 루프)
- [x] `plan-execute/SKILL.md` 생성
  - orchestration 로드 → 다음 Phase 선택 → 실행 → 검증 → 로그 업데이트
  - 완료 시 `docs/Plans/Completed/`로 이동 (필수)
  - 자동 호출: `/review-comments`, `/update-docs`
  - 완료 후 `/review-code`, `/commit` 권고

**의존성**: Step 3 완료 (SKILLS.md 티어 정의 + 참조 문서 경로 확정)

---

### Step 5: 신규 스킬 — Tier 2 검증 + 독립 도구

**목표**: 빌드/테스트 검증 스킬과 분석/계획 편집 스킬 생성.

- [x] `build/SKILL.md` 생성
  - CMake configure + build 실행
  - 빌드 에러 파싱 (MSVC/GCC 에러 패턴)
  - 출력: 성공(한 줄) / 실패(에러 테이블: file:line | error | message)
- [x] `test/SKILL.md` 생성
  - `cmake --build build --target unit-tests` + `ctest --output-on-failure`
  - Catch2 결과 파싱 (pass/fail/skip 카운트, 실패 상세)
  - 출력: 성공(한 줄) / 실패(테이블: test name | error message)
- [x] `analyze/SKILL.md` 생성
  - 읽기 전용: 코드 수정 없음
  - 대상 유형: 모듈, 클래스, Redis 키, 포트, Protobuf 메시지
  - 의존성 그래프 + 영향 범위 출력
  - C++ 적응: include 체인, Protobuf 참조, Redis 키 사용처, Docker 포트 매핑
- [x] `plan-edit/SKILL.md` 생성
  - 완료된 Phase 보호
  - 미실행 Phase만 수정/추가/삭제/재정렬
  - orchestration.md + execution-log.md 동기 업데이트
  - 번호 연속성, 파일 참조, 의존성 무결성 자동 검증

**의존성**: Step 4와 병렬 가능 (상호 독립). 단, SKILLS.md(Step 3)에 등록 필요.

---

### Step 6: CLAUDE.md 동기화 + 최종 검증

**목표**: CLAUDE.md를 새 구조에 맞게 업데이트하고 전체 정합성 검증.

- [x] CLAUDE.md 업데이트
  - Repository Structure 섹션: 새 docs 구조 반영
  - Documentation 섹션: `docs/Documentation-Checklist.md` 참조로 대체 (인라인 테이블 제거)
  - Skills 참조 추가 (선택: 사용 가능한 스킬 목록)
- [x] 전체 정합성 검증
  - 모든 스킬의 docs 경로가 실제 파일과 일치하는지 확인
  - SKILLS.md의 티어 정의가 각 스킬의 자동 호출 체인과 일치하는지 확인
  - Documentation-Checklist.md의 매핑 테이블이 실제 docs 구조와 일치하는지 확인
  - 기존 `.claude/settings.local.json`의 스킬 등록 상태 확인

**의존성**: Step 4, Step 5 모두 완료

---

## Step 간 의존성

```
Step 1 (폴더/파일 이동)
  │
  └──► Step 2 (신규 문서 작성)
         │
         └──► Step 3 (SKILLS.md + 기존 스킬 리뷰)
                │
                ├──► Step 4 (Tier 1 스킬 4개)  ──┐
                │                                │
                └──► Step 5 (Tier 2 + 독립 4개) ──┤
                                                  │
                                                  └──► Step 6 (CLAUDE.md + 검증)
```

Step 4와 Step 5는 병렬 진행 가능. 나머지는 순차.

---

## 변경 파일 요약

### Step 1: 폴더/파일 이동

| 변경 | 대상 |
|------|------|
| 폴더 생성 | `docs/Checklists/`, `docs/Systems/`, `docs/Plans/`, `docs/Plans/Completed/`, `docs/WorkLog/` |
| 파일 이동 | `docs/계획/*` → `docs/Plans/*` (5개 항목) |
| 파일 이동 | `docs/계획/로깅 통합 운영/` → `docs/Plans/Completed/로깅 통합 운영/` |
| 파일 이동 | `docs/Internal/구현기록/*.md` → `docs/WorkLog/YYYY-MM-DD/*.md` (13개 파일) |
| 폴더 제거 | `docs/계획/`, `docs/Internal/구현기록/` |

### Step 2: 신규 문서

| 파일 | 내용 |
|------|------|
| `docs/Documentation-Checklist.md` (신규) | 문서 매핑 + 작성 원칙 |
| `docs/Checklists/pattern-search-guide.md` (신규) | C++ 패턴 탐색 절차 |
| `docs/Checklists/review-code-checklist.md` (신규) | C++ 코드 리뷰 체크리스트 |
| `docs/Plans/계획-작성-가이드.md` (신규) | 계획 템플릿 |
| `docs/Systems/common 라이브러리.md` (신규) | common 모듈 상세 |
| `docs/Systems/Room Server.md` (신규) | Room Server 상세 |
| `docs/Systems/Chat Server.md` (신규) | Chat Server 상세 |
| `docs/Systems/인프라 구성.md` (신규) | 인프라 구성 상세 |

### Step 3: SKILLS.md + 기존 스킬 수정

| 파일 | 변경 |
|------|------|
| `.claude/skills/SKILLS.md` (신규) | 오케스트레이션 문서 |
| `.claude/skills/commit/SKILL.md` | 경로 업데이트 |
| `.claude/skills/review-code/SKILL.md` | 체크리스트 참조 분리 |
| `.claude/skills/review-comments/SKILL.md` | 경로 업데이트 |
| `.claude/skills/review-plan/SKILL.md` | 체크리스트 참조 추가 |
| `.claude/skills/update-docs/SKILL.md` | Documentation-Checklist 참조 |

### Step 4: Tier 1 스킬

| 파일 | 내용 |
|------|------|
| `.claude/skills/implement/SKILL.md` (신규) | 패턴 기반 구현 |
| `.claude/skills/debug/SKILL.md` (신규) | C++ 체계적 디버깅 |
| `.claude/skills/plan-create/SKILL.md` (신규) | 계획 생성 |
| `.claude/skills/plan-execute/SKILL.md` (신규) | 계획 순차 실행 |

### Step 5: Tier 2 + 독립 스킬

| 파일 | 내용 |
|------|------|
| `.claude/skills/build/SKILL.md` (신규) | CMake 빌드 + 에러 파싱 |
| `.claude/skills/test/SKILL.md` (신규) | CTest + Catch2 결과 파싱 |
| `.claude/skills/analyze/SKILL.md` (신규) | 의존성/영향 분석 |
| `.claude/skills/plan-edit/SKILL.md` (신규) | 계획 부분 수정 |

### Step 6: CLAUDE.md 동기화

| 파일 | 변경 |
|------|------|
| `CLAUDE.md` | Repository Structure, Documentation 섹션 업데이트 |

---

## 위험 요소

| 위험 | 영향 | 대응 |
|------|------|------|
| docs 경로 변경 시 기존 문서 간 상대 링크 깨짐 | 문서 내 링크 404 | Step 1에서 이동 후 모든 .md 파일의 상대 링크 일괄 검수 |
| Skills가 참조하는 docs 경로 불일치 | 스킬 실행 시 문서 로드 실패 | Step 6에서 전체 경로 정합성 검증 |
| Unity 스킬을 C++ 맥락에 단순 복사 | DOTS 전용 로직이 백엔드에 무의미 | 각 스킬마다 C++ 백엔드 맥락으로 재작성 (복사 금지) |
| build/test 스킬이 Windows 전용 | Docker/Linux 환경 미지원 | 스킬에 환경 감지 로직 포함 (Developer Command Prompt 필요 여부) |
